# Screenshots for Site Mapping Instructions

This directory should contain the following screenshot files:

## Required Screenshots

1. **step1-enable-ans-signin.png**
   - Shows Step 1: Enable ANS by signing into ANS Authentication
   - Should display the "Enable ANS" section with Step 1 visible
   - Shows the "Sign In to ANS" button and the warning message about browser cookies

2. **step2a-discover-services-agent-search.png**
   - Shows Step 2: Discover Services tab with "agent" searched
   - Should display the ANS Business Marketplace with:
     - Search bar containing "agent"
     - Fetch Status log showing API call details
     - Search results showing services matching "agent"
     - "Showing all X services" with service cards displayed

3. **step2b-site-mappings-form-filled.png**
   - Shows Step 2: Site Mappings tab with the form filled in
   - Should display:
     - URL Pattern field: `*.godaddy.com`
     - Service Type dropdown: 🔌 MCP Server (selected)
     - Select Service dropdown: agent (https://agent.tuneify.ai/mcp) (selected)
     - "+ Add Mapping" button visible

4. **step3-show-active-mappings.png**
   - Shows Step 3: Show Active Mappings
   - Should display:
     - Title: "Show Active Mappings (2)" with green circle containing "3"
     - Two mapping entries:
       - First: `godaddy.com` with checkbox checked, MCP icon, service "agent", URL `https://agent.tuneify.ai/mcp`, Delete button
       - Second: `*.godaddy.com` with checkbox checked, MCP icon, service "agent", URL `https://agent.tuneify.ai/mcp`, Delete button

## Screenshot Guidelines

- Use PNG format for best quality
- Ensure text is readable and clear
- Crop to show only the relevant section
- Maintain consistent sizing across all screenshots
- Use descriptive filenames as listed above

